# NOTE: Generated By HttpRunner v3.1.4
# FROM: testcases/device/device_auth_get_bind_params/device_auth_get_bind_params.yml


import pytest
from httprunner import Parameters


from httprunner import HttpRunner, Config, Step, RunRequest, RunTestCase


class TestCaseDeviceAuthGetBindParams(HttpRunner):
    @pytest.mark.parametrize(
        "param",
        Parameters(
            {
                "uuid": "${parameterize(test_data/normal_device_data.csv)}",
                "status_code-ok-status-code-ai_uuid": [[200, 1, "ok", 200, "AI"]],
            }
        ),
    )
    def test_start(self, param):
        super().test_start(param)

    config = Config("tange-iiCam365 设备获取绑定参数").base_url("${ENV(DV_test_url)}")

    teststeps = [
        Step(
            RunRequest("设备获取绑定参数")
            .post("device/auth/get_bind_params")
            .with_headers(**{"Content-Type": "application/json"})
            .with_json({"uuid": "$uuid"})
            .validate()
            .assert_equal("status_code", "$status_code")
            .assert_equal('headers."Content-Type"', "application/json;charset=UTF-8")
            .assert_equal("body.ok", "$ok")
            .assert_equal("body.status", "$status")
            .assert_equal("body.code", "$code")
            .assert_startswith("body.data.ai_uuid", "$ai_uuid")
        ),
    ]


if __name__ == "__main__":
    TestCaseDeviceAuthGetBindParams().test_start()
