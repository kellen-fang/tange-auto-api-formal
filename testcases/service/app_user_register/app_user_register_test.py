# NOTE: Generated By HttpRunner v3.1.4
# FROM: testcases/service/app_user_register/app_user_register.yml


import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).parent.parent.parent.parent))


import pytest
from httprunner import Parameters


from httprunner import HttpRunner, Config, Step, RunRequest, RunTestCase

from testcases.service.app_user_captcha_verify.app_user_captcha_verify_test import (
    TestCaseAppUserCaptchaVerify as AppUserCaptchaVerify,
)


class TestCaseAppUserRegister(HttpRunner):
    @pytest.mark.parametrize(
        "param",
        Parameters(
            {
                "username-scene-platform-channel-channel_id-captcha-pwd-language-pkgname-push_id-push_channel-version": "${parameterize(test_data/normal_register_data.csv)}",
                "status_code-ok-status-code": [[200, 1, "ok", 200]],
            }
        ),
    )
    def test_start(self, param):
        super().test_start(param)

    config = Config("账号注册").base_url("${ENV(api_url)}")

    teststeps = [
        Step(RunTestCase("校验验证码").call(AppUserCaptchaVerify)),
        Step(
            RunRequest("用户账号注册")
            .post("/app/user/register")
            .with_headers(**{"Content-Type": "application/json"})
            .with_json(
                {
                    "pkgname": "$pkgname",
                    "push_id": "$push_id",
                    "push_channel": "$push_channel",
                    "version": "$version",
                    "language": "$language",
                    "platform": "$platform",
                    "username": "$username",
                    "captcha": "$captcha",
                    "pwd": "$pwd",
                }
            )
            .validate()
            .assert_equal("status_code", "$status_code")
            .assert_equal('headers."Content-Type"', "application/json;charset=UTF-8")
            .assert_equal("body.ok", "$ok")
            .assert_equal("body.status", "$status")
            .assert_equal("body.code", "$code")
        ),
    ]


if __name__ == "__main__":
    TestCaseAppUserRegister().test_start()
